===
Raw text around a tag
===

<!-- Some HTML comment -->
#(title)
<p>And some tags here</p>

---

(source_file
  (raw_text_definition)
  (tag_definition
    (parameter_list
      (operator_parameter
        (identifier))))
  (raw_text_definition))

===
Raw text around and inside tags
===

<!-- Some HTML comment -->
#if(title) {
  <p>And some tags here</p>
  #(title)
}
<h1>And here, too!</h1>

---

(source_file
  (raw_text_definition)
  (tag_definition
    (name
        (identifier))
    (parameter_list
      (operator_parameter
        (identifier)))
    (body
      (definitions
        (raw_text_definition)
        (tag_definition
          (parameter_list
            (operator_parameter
              (identifier)))))))
  (raw_text_definition))
